# instructions/projects/

Per-project instruction files, routed by directory basename: a session whose
working directory is `~/Developer/acme-api` loads `acme-api.md` from here
(budget: 1,500 chars). Nothing loads for a project with no file — create one
only when a retro ships a project-scoped fix.
